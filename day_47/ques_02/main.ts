//let if = 5; // this line would cause syntax error

console.log(
    "Using a reserved word as a variable name cause a syntax error in  javascript."
);

//It's important to avoid using reserved word as identifiers to prevent these errors
