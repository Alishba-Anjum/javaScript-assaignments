function squareDecleratin(number:  number):number {
    return number * number;
}

const squareExpression = function(number: number):number{
    return number * number;
}
console.log(squareDecleratin(4));
console.log(squareExpression(4));

